#!/usr/bin/env python

from shutil import copyfile
import pip

copyfile('config.ini.example', 'config.ini')
print('Edit config.ini according to your profile')

pip.main(['install', '-r', 'requirements.txt'])
